{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.Hosting.Lifetime": "Information"
        }
    },
    "RabbitMq": {
        "Host": "localhost",
        "Port": 5673,
        "User": "guest",
        "Password": "guest",
        "QueueName": "readings"
    },
    "ConnectionStrings": {
        "Postgres": "Host=localhost;Port=5433;Database=meters;Username=postgres;Password=postgres"
    }
}
